

    <main>
       {{#if hasProducts }}
         {{#each prods}}
      <h1>My product</h1>
      <p>List of all product</p>
         {{/each}}
      {{else }}
         <h1>No Product Found!</h1>
      {{/if}}
    </main>